import { UploadDatePipe } from './upload-date.pipe';

describe('UploadDatePipe', () => {
  it('create an instance', () => {
    const pipe = new UploadDatePipe();
    expect(pipe).toBeTruthy();
  });
});
